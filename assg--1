#include<stdio.h>
main()
{
	int n,i,x[n];
	printf("Enter the no of numbers\n");
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&x[i]);
	}
	int min=x[0];
	for(i=1;i<n;i++)
	{
		if(min>x[i])
		{
			min=x[i];
		}
	}
	printf("The lowest among the given numbers is : %d",min);
	
}
